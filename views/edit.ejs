<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Edit your panda</title>
  
   
</head>
<body>

        <h1>Your Current Panda Info</h1>
        
        
        
            <p>
        
                <%= panda.panda %>
                    <%= panda.name %>
            </p>
        
        
        
           

            <h2>Edit your Panda</h2>
            <form action="/pandas/<%= panda.id %>" method="POST">

                <p>Your name:
                    <input type="text" name="name" value = "<%= panda.name %>"> </p>
                <p>Your panda:
                    <input type="text" name="panda" value = "<%= panda.panda %>"> </p>
                <button>update my panda!</button>

                    </form>
                




            <% if(typeof(errors) != 'undefined' ) { %>
                <% for (var x in errors) { %>
                    <h3>
                        <%= errors[x].message %>
                    </h3>
                    <% } %>
                        <% } %>
                               
            
    
</body>
</html>