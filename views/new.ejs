<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Panda new</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

</head>
<body>
    <form action="/pandas" method="POST">
        <% if(typeof(errors) != 'undefined' ) { %>
            <% for (var x in errors) { %>
                <h3>
                    <%= errors[x].message %>
                </h3>
                <% } %>
                    <% } %>
    
        <p>Your name:
            <input type="text" name="name"> </p>
        <p>Your panda:
            <input type="text" name="panda"> </p>
        <button>Add my panda!</button>
    </form>
    <!-- <a href="/quotes">
        <button>Skip to quotes!</button>
    </a> -->
</body>
</html>